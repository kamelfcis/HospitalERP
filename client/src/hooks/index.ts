/*
 * ═══════════════════════════════════════════════════════════════
 *  client/src/hooks/ — خريطة الـ Hooks المشتركة
 * ═══════════════════════════════════════════════════════════════
 *
 *  هذا المجلد يحتوي على hooks مشتركة بين أكثر من صفحة.
 *  كل hook له مسؤولية واحدة فقط.
 *
 *  ┌────────────────────────┬──────────────────────────────────────┐
 *  │ الملف                  │ الوظيفة                              │
 *  ├────────────────────────┼──────────────────────────────────────┤
 *  │ useAccounts.ts         │ جلب الحسابات من الـ API مع cache     │
 *  │                        │ useAccounts() — كل الحسابات          │
 *  │                        │ useRevenueAccounts() — الإيرادات فقط │
 *  ├────────────────────────┼──────────────────────────────────────┤
 *  │ useSSE.ts              │ إدارة اتصال SSE (Server-Sent Events) │
 *  │                        │ يفتح الاتصال، يربط Events،           │
 *  │                        │ ويُعيد المحاولة تلقائياً عند الخطأ  │
 *  ├────────────────────────┼──────────────────────────────────────┤
 *  │ use-auth.tsx           │ بيانات المستخدم الحالي (session)     │
 *  ├────────────────────────┼──────────────────────────────────────┤
 *  │ use-api-mutation.ts    │ wrapper مبسط لـ useMutation           │
 *  ├────────────────────────┼──────────────────────────────────────┤
 *  │ use-mobile.tsx         │ كشف حجم الشاشة (mobile/desktop)     │
 *  ├────────────────────────┼──────────────────────────────────────┤
 *  │ use-toast.ts           │ إظهار رسائل النجاح والخطأ            │
 *  ├────────────────────────┼──────────────────────────────────────┤
 *  │ useStableRequestId.ts  │ توليد request ID ثابت لمنع التكرار   │
 *  └────────────────────────┴──────────────────────────────────────┘
 *
 *  ملاحظة: hooks الخاصة بصفحة معينة توجد داخل مجلد الصفحة
 *  مثال: client/src/pages/cashier/hooks/useCashierShift.ts
 * ═══════════════════════════════════════════════════════════════
 */

export { useAccounts, useRevenueAccounts } from "./useAccounts";
export { useSSE } from "./useSSE";
export { useAuth } from "./use-auth";
export { useIsMobile } from "./use-mobile";
export { useToast } from "./use-toast";
